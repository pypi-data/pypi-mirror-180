Metadata-Version: 2.1
Name: chinillaclvm_rs
Version: 0.1.24
Summary: Implementation of `chinillaclvm` for Chinilla Network's cryptocurrency
Home-Page: https://github.com/Chinilla/chinillaclvm_rs/
Author: Richard Kiss <him@richardkiss.com>
Author-email: Richard Kiss <him@richardkiss.com>
License: Apache-2.0
Description-Content-Type: text/markdown; charset=UTF-8; variant=GFM
Project-URL: Source Code, https://github.com/Chinilla/chinillaclvm_rs/

The `chinillaclvm_rs` wheel has python bindings for the rust implementation of chinillaclvm.

Build
-----

Use `maturin` to build the python interface. First, install into current virtualenv with

```
$ pip install maturin
```

While in the `wheel` directory, build `chinillaclvm_rs` into the current virtualenv with

```
$ maturin develop --release
```

On UNIX-based platforms, you may get a speed boost on `sha256` operations by building
with OpenSSL.

```
$ maturin develop --release --features=openssl
```


To build the wheel, do

```
$ maturin build --release
````

or

```
$ maturin build --release --features=openssl
```

