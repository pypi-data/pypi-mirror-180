[tool.poetry]
name = "poetry-build-test"
version = "0.1.0"
description = ""
authors = ["Kashyap <KBhatt@StateStreet.com>"]
readme = "README.md"
packages = [{include = "poetry_build_test"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
