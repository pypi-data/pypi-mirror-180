[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jzgutil"
version = "1.2.0"
readme = "README.md"
dependencies = [
    'requests',
    'bs4',
    'click',
]