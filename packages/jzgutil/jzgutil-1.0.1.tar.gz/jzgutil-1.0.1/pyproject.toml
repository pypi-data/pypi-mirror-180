[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jzgutil"
version = "1.0.1"
readme = "README.md"
dependencies = [
    'requests',
    'bs4',
    'click',
]