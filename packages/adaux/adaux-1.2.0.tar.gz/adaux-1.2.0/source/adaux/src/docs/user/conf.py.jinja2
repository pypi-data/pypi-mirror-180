# pylint: disable=invalid-name, redefined-builtin
import sys
from pathlib import Path

sys.path.append(str(Path(__file__).resolve().parent{% for _ in aux.docs.root.split("/") %}.parent{% endfor %} / "devops/docs"))

# pylint: disable=import-error
from default_conf import *  # type: ignore # nopycln: import
