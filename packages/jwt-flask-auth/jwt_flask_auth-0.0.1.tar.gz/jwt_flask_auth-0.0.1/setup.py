import setuptools

setuptools.setup(
    name='jwt_flask_auth',
    version='0.0.1',
    author='Eteil Junior Djoumatchoua',
    description='Provide an  easy way to handle authentication(login with jwt generated, token required decorator) in flask applications',
    packages=['jwt_flask_auth']
)
