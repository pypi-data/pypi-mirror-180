[tox]
isolated_build = True
envlist = py37,
          py38,
          py39,
          pypi3
skip_missing_interpreters = true

[tox:.package]
basepython = python3

[testenv]
deps =
    pytest
    pytest-httpx
    pytest-mock
description = Run the unit tests using {basepython}
commands = python -m pytest
