[tool.poetry]
name = "fish-dbjob"
version = "0.2.1"
description = ""
authors = ["Tim Chen <firstim@gmail.com>"]
readme = "README.md"
packages = [{include = "fish_dbjob"}]

[tool.poetry.scripts]
fish-dbjob = "fish_dbjob.cli:app"

[tool.poetry.dependencies]
python = "^3.7"
typer = {extras = ["all"], version = "^0.7.0"}
databricks-cli = "^0.17.4"
tomli = "^2.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
