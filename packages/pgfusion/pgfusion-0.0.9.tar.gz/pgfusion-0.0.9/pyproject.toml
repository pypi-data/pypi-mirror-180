[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pgfusion"
version = "0.0.9"
description = "An asyncpg wrapper with a simple api"
readme = "README.md"
requires-python = ">=3.10.4"
license = { text = 'MIT' }
authors = [{ name = "Wayde Gilliam", email = "waydegilliam@gmail.com" }]
keywords = []
classifiers = []
dependencies = ["asyncpg==0.27.0"]

[tool.hatch.build]
only-packages = true
