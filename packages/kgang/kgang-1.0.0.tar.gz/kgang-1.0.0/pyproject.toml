[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kgang"
version = "1.0.0"
readme = "README.md"
dependencies = [
    'requests',
    'bs4',
    'click',
]