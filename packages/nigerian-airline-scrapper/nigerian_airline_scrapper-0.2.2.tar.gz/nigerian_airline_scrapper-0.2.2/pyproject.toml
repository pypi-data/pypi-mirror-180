# pyproject.toml

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nigerian-airline-scrapper"
version = "0.2.2"
description = ""
readme = "README.md"
authors = [{ name = "Atinuke", email = "Ayoyinkaomotoso@gmail.com" }]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = ["airline", "scrapper", "nigerian"]
dependencies = [
    "feedparser >= 5.2.0",
    "html2text",
    "BeautifulSoup4",
    "lxml",
    'tomli; python_version < "3.11"',
]
requires-python = ">=3.6"