from setuptools import setup

setup(name='feris-first-pypi-package',
      version='0.1',
      description='Udacity Gaussian distributions',
      packages=['feris-first-pypi-package'],
      zip_safe=False)
