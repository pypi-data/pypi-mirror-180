from setuptools import setup

setup(name='feris_first_pypi_package',
      version='0.2',
      description='Udacity Gaussian distributions',
      packages=['feris_first_pypi_package'],
      zip_safe=False)
