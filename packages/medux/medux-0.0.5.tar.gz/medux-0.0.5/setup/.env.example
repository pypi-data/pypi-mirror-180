DEBUG=False
SECRET_KEY=${SECRET_KEY}

# postgresql, mysql, sqlite3, oracle:
DATABASE_ENGINE=django.db.backends.${DATABASE_ENGINE}
DATABASE_NAME=medux
DATABASE_USER=${MEDUX_USER}
DATABASE_PASS=${DB_PASSWORD}

# your hostname / FQDN
ALLOWED_HOSTS=127.0.0.1,localhost,${DOMAIN}